
import java.awt.*;
import java.awt.event.*;
import javax.swing.*;
public class calculator extends JFrame {
	private JTextField jtfResult = new JTextField();
	private JButton jbt[];
	
	public calculator(){
		JPanel p1 = new JPanel(new GridLayout(1,1) );
		p1.add(jtfResult);
		JPanel p2 = new JPanel(new GridLayout(8,4) );
	}
	
}
